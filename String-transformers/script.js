 lowercaseop.innerText = input.value.toLowerCase()
 uppercaseop.innerText = input.value.toUpperCase()

 //adding logic so that typing happens with every word typed as input

 input.addEventListener('input',(e) => {
    lowercaseop.innerText = input.value.toLowerCase()
    uppercaseop.innerText = input.value.toUpperCase()
    camelcaseop.innerText = input.value.
    pascalcaseop.innerText = input.value.
    snakecaseop.innerText = input.value.
    kebabcaseop.innerText = input.value.
    trimop.innerText = input.value.

 })

 //creatting a function for camel case typing 

 function toCamelCase(){
      
 }
